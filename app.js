/*const products = document.querySelectorAll(".prod__img");
const prodBg = document.querySelectorAll(".product__Bg");

let x = window.matchMedia("(max-width:1400px)");

function changeHeight() {
  for (i of prodBg) {
    for (h of products) {
      if (x.matches) {
        let prodHeight = h.offsetHeight;

        i.style.height = `${prodHeight}px`;
        console.log(i);
      } else {
        i.style.height = "232px";
      }
    }
  }
}

changeHeight();
window.addEventListener("resize", changeHeight);
*/
